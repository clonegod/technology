package clonegod.network.netty.rpc.provider;

public class PRCServiceProvider {
	
	// TODO 
	/***
	 * 服务提供方 向 注册中心 注册服务
	 * provider -> registry
	 * 
	 * 客户端 从 注册中心 查询服务地址
	 * consumer -> registry
	 * 
	 * 客户端 根据 注册中心 返回的服务提供方地址，调用服务
	 * consumer -> 参数序列化 -> provider
	 * 
	 */
}
